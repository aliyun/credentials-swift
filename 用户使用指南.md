# Alibaba Cloud Credentials Swift SDK 用户使用指南

## 1. 项目简介

### 1.1 项目用途
Alibaba Cloud Credentials for Swift 是阿里云官方提供的 Swift 语言凭证管理 SDK，用于为阿里云 API 调用提供安全的身份验证凭证。该 SDK 简化了多种凭证类型的管理和自动刷新机制，使开发者能够更安全、便捷地访问阿里云服务。

### 1.2 核心能力
- **多种凭证类型支持**：支持 AccessKey、STS 临时凭证、Bearer Token、ECS RAM 角色、RAM 角色扮演（AssumeRole）、RSA 密钥对、OIDC 角色扮演、自定义 URI 凭证等多种认证方式
- **自动凭证刷新**：对于临时凭证（如 STS、ECS RAM 角色等），SDK 会在凭证过期前 180 秒自动刷新，无需手动管理
- **统一凭证接口**：通过统一的 Client 接口获取不同类型的凭证，简化代码逻辑
- **安全性保障**：支持网络超时配置、代理设置等安全特性

### 1.3 典型应用场景
1. **服务端应用**：使用 AccessKey 或 RAM 角色进行长期身份验证
2. **临时授权场景**：通过 STS 临时凭证为移动端或前端应用提供短期访问权限
3. **ECS 实例**：在阿里云 ECS 实例上使用实例 RAM 角色，无需硬编码凭证
4. **跨账号访问**：使用 RAM 角色扮演（AssumeRole）实现跨账号资源访问
5. **第三方集成**：通过 OIDC 角色扮演实现与外部身份提供商的集成
6. **容器化环境**：使用 URI 凭证服务在容器环境中动态获取凭证

---

## 2. 使用前提与环境要求

### 2.1 操作系统要求
- **macOS**: 10.15 (Catalina) 或更高版本
- **iOS**: 13.0 或更高版本
- **tvOS**: 13.0 或更高版本
- **watchOS**: 6.0 或更高版本
- **Linux**: 支持 Swift 运行环境的发行版（如 Ubuntu 18.04+）

### 2.2 运行时环境
- **Swift 版本**: 5.6 或更高版本（必须）
- **Xcode**: 11.3 或更高版本（macOS/iOS 开发）

### 2.3 依赖库
SDK 依赖以下第三方库，安装时会自动引入：
- **CryptoSwift** (>= 1.5.1)：用于加密签名计算
- **Tea** (>= 1.0.3)：阿里云 Tea DSL 框架，提供 HTTP 请求支持

### 2.4 网络要求
根据使用的凭证类型，可能需要以下网络访问权限：

| 凭证类型 | 网络要求 |
|---------|---------|
| AccessKey | 无需网络访问 |
| STS | 无需网络访问（仅创建凭证对象） |
| Bearer Token | 无需网络访问 |
| ECS RAM 角色 | 需访问 ECS 元数据服务 `100.100.100.200` |
| RAM 角色扮演 | 需访问 `sts.aliyuncs.com` |
| RSA 密钥对 | 需访问 `sts.aliyuncs.com` |
| OIDC 角色扮演 | 需访问 `sts.aliyuncs.com` |
| URI 凭证 | 需访问自定义的凭证服务器 |

### 2.5 权限要求
- **ECS RAM 角色**：应用必须运行在已绑定 RAM 角色的阿里云 ECS 实例上
- **RAM 角色扮演**：需要具有 `AssumeRole` 权限的 AccessKey
- **RSA 密钥对**：需要具有 `GenerateSessionAccessKey` 权限的公钥/私钥对
- **OIDC 角色扮演**：需要配置 OIDC 身份提供商并具有相应的信任策略

---

## 3. 快速开始指南

### 3.1 安装方法

#### 3.1.1 使用 CocoaPods
在项目的 `Podfile` 中添加：

```ruby
pod 'AlibabaCloudCredentials', '~> 1.0.0'
```

然后执行：
```bash
pod install
```

#### 3.1.2 使用 Carthage
在 `Cartfile` 中添加：

```ogdl
github "aliyun/credentials-swift" "1.0.0"
```

然后执行：
```bash
carthage update --platform iOS
```

#### 3.1.3 使用 Swift Package Manager
在 `Package.swift` 中添加依赖：

```swift
dependencies: [
    .package(url: "https://github.com/aliyun/credentials-swift.git", from: "1.0.0")
]
```

并在 target 中添加：
```swift
.target(
    name: "YourTargetName",
    dependencies: ["AlibabaCloudCredentials"]
)
```

然后执行：
```bash
swift build
```

### 3.2 配置说明

#### 3.2.1 使用 AccessKey 凭证（长期凭证）
```swift
import AlibabaCloudCredentials

let config = Config([
    "type": "access_key",
    "accessKeyId": "your-access-key-id",
    "accessKeySecret": "your-access-key-secret"
])

let client = Client(config)
let credential = try await client.getCredential()
print("AccessKeyId: \(credential?.getAccessKeyId() ?? "")")
```

#### 3.2.2 使用 STS 临时凭证
```swift
let config = Config([
    "type": "sts",
    "accessKeyId": "your-sts-access-key-id",
    "accessKeySecret": "your-sts-access-key-secret",
    "securityToken": "your-sts-security-token"
])

let client = Client(config)
let credential = try await client.getCredential()
```

#### 3.2.3 使用 ECS RAM 角色（推荐在 ECS 上使用）
```swift
let config = Config([
    "type": "ecs_ram_role",
    "roleName": "your-ecs-role-name"  // 可选，不指定时自动获取
])

let client = Client(config)
let credential = try await client.getCredential()
```

#### 3.2.4 使用 RAM 角色扮演
```swift
let config = Config([
    "type": "ram_role_arn",
    "accessKeyId": "your-access-key-id",
    "accessKeySecret": "your-access-key-secret",
    "roleArn": "acs:ram::account-id:role/role-name",
    "roleSessionName": "session-name",  // 可选
    "policy": "{}",  // 可选，额外的权限策略
    "roleSessionExpiration": 3600  // 可选，会话有效期（秒）
])

let client = Client(config)
let credential = try await client.getCredential()
```

### 3.3 启动与验证

#### 3.3.1 基本验证
```swift
import AlibabaCloudCredentials

// 创建配置
let config = Config([
    "type": "access_key",
    "accessKeyId": "test-ak-id",
    "accessKeySecret": "test-ak-secret"
])

// 创建客户端
let client = Client(config)

// 获取凭证（async/await 方式）
Task {
    do {
        let accessKeyId = try await client.getAccessKeyId()
        let accessKeySecret = try await client.getAccessKeySecret()
        print("✓ 凭证获取成功")
        print("  AccessKeyId: \(accessKeyId)")
        print("  凭证类型: \(client.getType())")
    } catch {
        print("✗ 凭证获取失败: \(error)")
    }
}
```

#### 3.3.2 预期输出示例
成功时输出：
```
✓ 凭证获取成功
  AccessKeyId: test-ak-id
  凭证类型: access_key
```

失败时输出示例：
```
✗ 凭证获取失败: EmptyOrNil("accessKeyId or accessKeySecret cannot be empty.")
```

### 3.4 配置参数说明

| 参数名 | 类型 | 必填 | 适用凭证类型 | 说明 |
|-------|------|------|-------------|------|
| `type` | String | 是 | 所有 | 凭证类型：`access_key`、`sts`、`bearer`、`ecs_ram_role`、`ram_role_arn`、`rsa_key_pair`、`oidc_role_arn`、`credentials_uri` |
| `accessKeyId` | String | 条件 | access_key, sts, ram_role_arn | AccessKey ID |
| `accessKeySecret` | String | 条件 | access_key, sts, ram_role_arn | AccessKey Secret |
| `securityToken` | String | 是 | sts | STS 临时安全令牌 |
| `bearerToken` | String | 是 | bearer | Bearer Token |
| `roleName` | String | 否 | ecs_ram_role | ECS RAM 角色名称（不指定时自动获取） |
| `roleArn` | String | 是 | ram_role_arn, oidc_role_arn | RAM 角色的 ARN |
| `roleSessionName` | String | 否 | ram_role_arn, oidc_role_arn | 角色会话名称，默认 "defaultSessionName" |
| `roleSessionExpiration` | Int | 否 | ram_role_arn, rsa_key_pair, oidc_role_arn | 会话有效期（秒），默认 3600 |
| `policy` | String | 否 | ram_role_arn | 额外的权限策略 JSON |
| `publicKeyId` | String | 是 | rsa_key_pair | RSA 公钥 ID |
| `privateKeySecret` | String | 是 | rsa_key_pair | RSA 私钥内容 |
| `oidcProviderArn` | String | 是 | oidc_role_arn | OIDC 身份提供商的 ARN |
| `oidcTokenFilePath` | String | 是 | oidc_role_arn | OIDC Token 文件路径 |
| `credentialsURI` | String | 是 | credentials_uri | 凭证服务器 URI |
| `timeout` | Int | 否 | 所有网络请求 | 读取超时（毫秒），默认 1000 |
| `connectTimeout` | Int | 否 | 所有网络请求 | 连接超时（毫秒），默认 1000 |
| `proxy` | String | 否 | 所有网络请求 | 代理服务器地址 |
| `regionId` | String | 否 | ram_role_arn, rsa_key_pair | 区域 ID，默认 "cn-hangzhou" |

---

## 4. 常见问题解答（FAQ）

### 4.1 凭证配置相关问题

#### 问题 4.1.1：配置 AccessKey 凭证时提示 "accessKeyId or accessKeySecret cannot be empty"

**问题现象**：
```
Error: EmptyOrNil("accessKeyId or accessKeySecret cannot be empty.")
```

**根本原因**：
代码在 `AccessKeyCredential` 初始化时会检查 `accessKeyId` 和 `accessKeySecret` 是否为空字符串（见 `Credentials.swift` 第 46-48 行）。如果任一参数为空字符串或 nil，会抛出 `EmptyOrNil` 异常。

**解决方案**：
1. 确保配置中提供了有效的 `accessKeyId` 和 `accessKeySecret`
2. 检查配置字典的键名是否正确（注意大小写）
3. 验证从环境变量或配置文件读取的值不为空

```swift
// ✗ 错误示例
let config = Config([
    "type": "access_key",
    "accessKeyId": "",  // 空字符串会导致错误
    "accessKeySecret": "secret"
])

// ✓ 正确示例
let config = Config([
    "type": "access_key",
    "accessKeyId": "LTAI5tAbc123...",
    "accessKeySecret": "abc123xyz..."
])
```

#### 问题 4.1.2：配置 STS 凭证时提示 "securityToken cannot be empty"

**问题现象**：
```
Error: EmptyOrNil("accessKeyId or accessKeySecret cannot be empty.")
```

**根本原因**：
`StsCredential` 在初始化时会验证 `accessKeyId`、`accessKeySecret` 和 `securityToken` 三个参数都不能为空（见 `Credentials.swift` 第 92-94 行）。

**解决方案**：
确保 STS 凭证的三个核心参数都已正确设置：

```swift
let config = Config([
    "type": "sts",
    "accessKeyId": "STS.xxx",
    "accessKeySecret": "xxx",
    "securityToken": "CAISxxx..."  // 必须提供
])
```

#### 问题 4.1.3：使用不支持的凭证类型

**问题现象**：
```
Error: UnsupportedCredentialType("unknown_type")
```

**根本原因**：
代码在 `Client.getCredential()` 中通过 switch-case 处理凭证类型（见 `Client.swift` 第 15-32 行）。如果 `type` 参数不匹配任何已知类型，会抛出 `UnsupportedCredentialType` 异常。

**解决方案**：
使用以下支持的凭证类型之一：
- `access_key` - AccessKey 凭证
- `sts` - STS 临时凭证
- `bearer` - Bearer Token
- `ecs_ram_role` - ECS RAM 角色
- `ram_role_arn` - RAM 角色扮演
- `rsa_key_pair` - RSA 密钥对
- `oidc_role_arn` - OIDC 角色扮演
- `credentials_uri` - URI 凭证服务

```swift
// ✗ 错误
let config = Config(["type": "ak"])  // 不支持

// ✓ 正确
let config = Config(["type": "access_key"])
```

### 4.2 ECS RAM 角色相关问题

#### 问题 4.2.1：在非 ECS 环境使用 ECS RAM 角色导致连接超时

**问题现象**：
应用长时间无响应或抛出网络连接超时错误。

**根本原因**：
ECS RAM 角色凭证提供者会尝试连接 `100.100.100.200`（ECS 元数据服务地址）获取角色凭证（见 `Providers.swift` 第 28 行）。该地址仅在阿里云 ECS 实例内网可访问，在本地开发环境或其他云环境无法访问，导致连接超时（默认超时 1000ms）。

**解决方案**：
1. **仅在 ECS 实例上使用此凭证类型**
2. 本地开发时使用 AccessKey 或 STS 凭证
3. 可通过环境变量动态切换凭证类型：

```swift
#if DEBUG
let config = Config([
    "type": "access_key",
    "accessKeyId": "local-test-ak",
    "accessKeySecret": "local-test-secret"
])
#else
let config = Config([
    "type": "ecs_ram_role",
    "roleName": "MyEcsRole"
])
#endif
```

#### 问题 4.2.2：ECS 实例元数据服务返回 404 错误

**问题现象**：
```
Error: InvalidData("The role name was not found in the instance")
```

**根本原因**：
代码在获取 RAM 角色名称时，如果元数据服务返回 404 状态码，说明 ECS 实例未绑定 RAM 角色（见 `Providers.swift` 第 56-58 行）。

**解决方案**：
1. 在阿里云控制台为 ECS 实例绑定 RAM 角色
2. 步骤：ECS 控制台 → 选择实例 → 更多 → 实例设置 → 授予/收回 RAM 角色
3. 绑定后需重启应用（无需重启 ECS 实例）

#### 问题 4.2.3：ECS RAM 角色凭证获取返回非 200 状态码

**问题现象**：
```
Error: InvalidData("Failed to get RAM session credentials from ECS metadata service. HttpCode=500")
```

**根本原因**：
元数据服务在两个阶段都会检查 HTTP 状态码（见 `Providers.swift` 第 59-61 行和 68-70 行）：
- 获取角色名称时
- 获取角色凭证时

非 200 状态码可能由于网络问题、服务暂时不可用或权限配置错误。

**解决方案**：
1. 检查 ECS 实例的网络配置，确保安全组允许访问元数据服务
2. 验证 RAM 角色的信任策略是否正确配置
3. 增加重试逻辑或调整超时时间：

```swift
let config = Config([
    "type": "ecs_ram_role",
    "roleName": "MyRole",
    "connectTimeout": 5000,  // 增加到 5 秒
    "timeout": 5000
])
```

#### 问题 4.2.4：ECS 元数据返回的 JSON 中 Code 字段不是 "Success"

**问题现象**：
```
Error: RequestError(["Code": "InvalidParameter", "Message": "..."])
```

**根本原因**：
代码会解析元数据服务返回的 JSON，检查 `Code` 字段是否为 "Success"（见 `Providers.swift` 第 74 行）。如果不是，会将整个响应作为错误抛出。

**解决方案**：
1. 查看错误信息中的 `Message` 字段了解具体原因
2. 常见原因包括：
   - RAM 角色策略配置错误
   - RAM 角色信任策略未包含 ECS 服务
   - 角色名称拼写错误

### 4.3 RAM 角色扮演相关问题

#### 问题 4.3.1：RAM 角色扮演时提示 AccessKey 不能为空

**问题现象**：
```
Error: EmptyOrNil("accessKeyId or accessKeySecret cannot be empty.")
```

**根本原因**：
`RamRoleArnCredentialProvider` 在初始化时强制要求提供 `accessKeyId` 和 `accessKeySecret`（见 `Providers.swift` 第 102-104 行）。这是因为角色扮演需要使用一个基础凭证来调用 AssumeRole API。

**解决方案**：
提供具有 `AssumeRole` 权限的 AccessKey：

```swift
let config = Config([
    "type": "ram_role_arn",
    "accessKeyId": "LTAI5t...",  // 必须提供
    "accessKeySecret": "xxx...",  // 必须提供
    "roleArn": "acs:ram::123456789:role/MyRole"
])
```

#### 问题 4.3.2：AssumeRole 调用失败返回错误信息

**问题现象**：
```
Error: RequestError(["Code": "NoPermission", "Message": "You are not authorized to do this action."])
```

**根本原因**：
代码通过检查响应中是否包含 "Credentials" 键来判断 AssumeRole 是否成功（见 `Providers.swift` 第 151 行）。失败时会将完整的错误响应抛出。

**解决方案**：
1. **NoPermission 错误**：确保基础 AccessKey 具有 `sts:AssumeRole` 权限
2. **InvalidParameter.RoleArn**：检查 `roleArn` 格式是否正确，应为 `acs:ram::account-id:role/role-name`
3. **EntityNotExist.Role**：验证角色是否存在
4. **NoPermission.AssumeRole**：检查角色的信任策略是否允许当前用户扮演

示例权限策略：
```json
{
  "Version": "1",
  "Statement": [{
    "Effect": "Allow",
    "Action": "sts:AssumeRole",
    "Resource": "acs:ram::123456789:role/MyRole"
  }]
}
```

#### 问题 4.3.3：获取凭证时强制类型转换失败导致崩溃

**问题现象**：
应用直接崩溃，控制台显示类似：
```
Fatal error: Unexpectedly found nil while unwrapping an Optional value
```

**根本原因**：
代码中多处使用了强制类型转换 `as!`（见 `Providers.swift` 第 153-156 行）。如果 STS 响应的 JSON 结构与预期不符（如缺少某些字段或类型不匹配），会导致强制解包失败而崩溃。

**解决方案**：
这是 SDK 的设计问题，用户层面难以规避。建议：
1. 使用 do-catch 捕获所有异常
2. 在开发环境充分测试
3. 向阿里云提交工单报告问题

```swift
do {
    let credential = try await client.getCredential()
    // 使用凭证
} catch {
    print("凭证获取失败: \(error)")
    // 降级处理或记录日志
}
```

### 4.4 RSA 密钥对相关问题

#### 问题 4.4.1：RSA 密钥对参数缺失

**问题现象**：
```
Error: EmptyOrNil("You must provide a valid pair of Public Key ID(Config.publicKeyId) and Private Key Secret(Config.privateKeySecret).")
```

**根本原因**：
`RsaKeyPairCredentialProvider` 在初始化时会检查 `publicKeyId` 和 `privateKeySecret` 是否都非空（见 `Providers.swift` 第 176-180 行）。

**解决方案**：
确保同时提供公钥 ID 和私钥内容：

```swift
let config = Config([
    "type": "rsa_key_pair",
    "publicKeyId": "PUBKEY123...",
    "privateKeySecret": "-----BEGIN RSA PRIVATE KEY-----\nMIIE...\n-----END RSA PRIVATE KEY-----"
])
```

#### 问题 4.4.2：GenerateSessionAccessKey API 调用失败

**问题现象**：
```
Error: RequestError(["Code": "InvalidAccessKeyId.NotFound", ...])
```

**根本原因**：
代码调用 STS 的 `GenerateSessionAccessKey` API 时需要使用有效的 RSA 密钥对（见 `Providers.swift` 第 189-216 行）。响应中如果不包含 "SessionAccessKey" 键，会抛出错误（第 219 行）。

**解决方案**：
1. 确认公钥 ID 与私钥匹配
2. 验证密钥对未被删除或禁用
3. 检查私钥格式是否正确（需完整的 PEM 格式）
4. 确认账户已启用 RSA 密钥对功能

### 4.5 URI 凭证相关问题

#### 问题 4.5.1：凭证 URI 为空

**问题现象**：
```
Error: EmptyOrNil("Credential URI cannot be empty.")
```

**根本原因**：
`URICredentialProvider` 初始化时强制要求提供非空的 `credentialsURI`（见 `Providers.swift` 第 239-241 行）。

**解决方案**：
提供有效的凭证服务器地址：

```swift
let config = Config([
    "type": "credentials_uri",
    "credentialsURI": "http://your-credential-server.com/get-credentials"
])
```

#### 问题 4.5.2：URI 凭证服务器返回非 2xx 状态码

**问题现象**：
```
Error: InvalidData("Failed to get credentials from server: http://example.com/creds\nHttpStatusCode=403\nHttpRAWContent={...}")
```

**根本原因**：
代码会检查 HTTP 响应状态码是否在 200-299 范围内（见 `Providers.swift` 第 259-261 行）。非 2xx 状态码会抛出详细的错误信息，包含状态码和响应体。

**解决方案**：
1. **403 错误**：检查服务器访问权限，确保客户端 IP 在白名单内
2. **404 错误**：验证 URI 路径是否正确
3. **500 错误**：检查凭证服务器日志，修复服务端问题
4. 实施重试逻辑：

```swift
func getCredentialWithRetry(maxRetries: Int = 3) async throws -> Credential? {
    for attempt in 1...maxRetries {
        do {
            return try await client.getCredential()
        } catch {
            if attempt == maxRetries { throw error }
            try await Task.sleep(nanoseconds: UInt64(attempt) * 1_000_000_000)  // 指数退避
        }
    }
    return nil
}
```

#### 问题 4.5.3：URI 返回的 JSON 格式不符合预期

**问题现象**：
```
Error: RequestError(["Code": "Failed", ...])
```

**根本原因**：
代码期望 URI 凭证服务返回的 JSON 包含 `Code: "Success"` 字段（见 `Providers.swift` 第 265 行）。服务器必须严格按照以下格式返回：

```json
{
  "Code": "Success",
  "AccessKeyId": "STS.xxx",
  "AccessKeySecret": "xxx",
  "SecurityToken": "CAISxxx...",
  "Expiration": "2024-01-01T12:00:00Z"
}
```

**解决方案**：
1. 调整凭证服务器，确保返回正确的 JSON 格式
2. `Code` 字段必须是字符串 `"Success"`
3. `Expiration` 必须是 ISO 8601 格式（UTC 时间）：`yyyy-MM-dd'T'HH:mm:ss'Z'`
4. 所有字段都必须存在，否则会因强制类型转换崩溃

### 4.6 凭证刷新与过期问题

#### 问题 4.6.1：临时凭证未自动刷新

**问题现象**：
凭证过期后仍使用旧凭证，导致 API 调用失败。

**根本原因**：
SDK 通过 `shouldRefresh()` 方法判断是否需要刷新凭证（见 `Providers.swift` 第 9-11 行）。刷新条件是：剩余有效期 ≤ 180 秒。如果未调用 `getCredential()`，不会触发刷新。

**解决方案**：
1. 在每次 API 调用前调用 `client.getCredential()`，SDK 会自动处理刷新
2. 不要缓存 `Credential` 对象，而是缓存 `Client` 对象
3. 对于高频调用场景，可手动检查过期时间：

```swift
class CredentialManager {
    let client: Client
    private var lastRefresh: Date = Date()
    
    func getCredential() async throws -> Credential? {
        // SDK 内部会自动判断是否需要刷新（过期前 180 秒）
        return try await client.getCredential()
    }
}
```

#### 问题 4.6.2：时间格式解析失败导致凭证刷新异常

**问题现象**：
应用崩溃，提示：
```
Fatal error: Unexpectedly found nil while unwrapping an Optional value
```
（在日期转换相关代码）

**根本原因**：
代码使用 `convertToDate()` 扩展方法解析时间字符串，内部使用强制解包 `!`（见 `Extensions.swift` 第 29 行）。如果时间字符串格式不匹配 `yyyy-MM-dd'T'HH:mm:ss'Z'`，解析失败会崩溃。

**解决方案**：
这是 SDK 的缺陷，用户层面难以防护。建议：
1. 确保自定义凭证服务器严格返回 ISO 8601 UTC 时间格式
2. 向阿里云提交问题反馈
3. 在调试模式下启用 catch-all 异常处理

### 4.7 网络与超时问题

#### 问题 4.7.1：网络请求超时

**问题现象**：
应用长时间挂起后抛出超时错误。

**根本原因**：
SDK 的默认超时设置较短：连接超时和读取超时都是 1000ms（见 `Providers.swift` 中各 Provider 的初始化）。在网络环境较差或跨地域访问时可能不足。

**解决方案**：
通过配置增加超时时间：

```swift
let config = Config([
    "type": "ram_role_arn",
    "accessKeyId": "xxx",
    "accessKeySecret": "xxx",
    "roleArn": "xxx",
    "connectTimeout": 10000,  // 连接超时 10 秒
    "timeout": 10000          // 读取超时 10 秒
])
```

建议值：
- 内网环境：3000-5000ms
- 公网环境：10000-15000ms
- 跨国访问：15000-30000ms

#### 问题 4.7.2：代理配置不生效

**问题现象**：
设置了代理参数但请求仍直连目标服务器。

**根本原因**：
当前 SDK 代码中虽然有 `proxy` 配置字段（见 `Models.swift` 第 18 行），但在 Providers 的实现中并未实际使用该参数传递给底层 HTTP 请求库。

**解决方案**：
这是 SDK 功能缺失，建议：
1. 在系统级别配置代理（macOS 系统偏好设置 / iOS VPN 配置）
2. 向阿里云提交功能需求
3. 如需强制代理，可 fork SDK 修改 `Tea.TeaCore.doAction` 调用部分

### 4.8 并发与线程安全问题

#### 问题 4.8.1：多线程同时调用导致凭证状态异常

**问题现象**：
在高并发场景下，偶发获取到空凭证或凭证字段错乱。

**根本原因**：
`CredentialsProvider` 类中的 `credential` 和 `expiration` 属性未加锁保护（见 `Providers.swift` 第 6-7 行）。多个线程同时调用 `getCredential()` 可能导致竞态条件。

**解决方案**：
1. **单例模式**：确保 `Client` 实例在全局只有一个
2. **串行队列**：通过 DispatchQueue 串行化凭证获取操作

```swift
actor CredentialManager {
    private let client: Client
    
    init(config: Config) {
        self.client = Client(config)
    }
    
    func getCredential() async throws -> Credential? {
        // actor 自动保证线程安全
        return try await client.getCredential()
    }
}

// 使用
let manager = CredentialManager(config: config)
let credential = try await manager.getCredential()
```

### 4.9 异常处理最佳实践

#### 问题 4.9.1：如何正确处理所有可能的异常

**解决方案**：
SDK 主要抛出以下几种异常（见 `Credentials.swift` 第 3-8 行）：

```swift
do {
    let credential = try await client.getCredential()
    // 使用凭证
} catch CredentialException.UnsupportedCredentialType(let type) {
    print("不支持的凭证类型: \(type)")
    // 检查配置中的 type 字段
} catch CredentialException.EmptyOrNil(let message) {
    print("参数缺失或为空: \(message)")
    // 检查必填参数
} catch CredentialException.InvalidData(let message) {
    print("数据无效: \(message)")
    // 检查网络响应或元数据服务
} catch CredentialException.RequestError(let response) {
    print("请求失败: \(response ?? [:])")
    // 查看具体错误码和消息
} catch {
    print("未知错误: \(error)")
    // 记录完整错误信息用于排查
}
```

### 4.10 调试与诊断

#### 问题 4.10.1：如何查看详细的网络请求日志

**解决方案**：
SDK 底层使用 Tea 框架，可通过环境变量或调试工具查看请求：

1. **使用网络代理工具**（Charles、Wireshark）抓包分析
2. **添加日志打印**：

```swift
let config = Config([
    "type": "ram_role_arn",
    // ... 其他配置
])

print("配置信息: \(config.toMap())")

do {
    let credential = try await client.getCredential()
    print("凭证获取成功: AK=\(credential?.getAccessKeyId() ?? "nil")")
} catch {
    print("错误详情: \(error)")
    print("错误类型: \(type(of: error))")
}
```

3. **断点调试**：在 Xcode 中在 `Providers.swift` 的关键位置设置断点

#### 问题 4.10.2：如何验证凭证是否有效

**解决方案**：
获取凭证后，调用阿里云 API 验证：

```swift
import AlibabaCloudCredentials

// 获取凭证
let client = Client(config)
let credential = try await client.getCredential()

// 使用凭证调用阿里云 API（以 ECS DescribeRegions 为例）
// 如果调用成功，说明凭证有效
// 具体 API 调用需使用对应的 SDK（如 alibabacloud-ecs-swift）
```

### 4.11 版本兼容性问题

#### 问题 4.11.1：Swift 版本不兼容

**问题现象**：
编译时出现语法错误或特性不支持。

**根本原因**：
SDK 要求 Swift 5.6 或更高版本（见 `Package.swift` 第 1 行和 `.podspec` 第 22 行）。使用旧版本会导致编译失败。

**解决方案**：
1. 更新 Xcode 到 13.3 或更高版本
2. 检查 Swift 版本：`swift --version`
3. 在 Xcode 项目设置中确认 Swift Language Version 为 5.6+

#### 问题 4.11.2：依赖库版本冲突

**问题现象**：
```
error: package dependency has different version requirements
```

**根本原因**：
项目中其他依赖可能要求不同版本的 CryptoSwift 或 Tea。

**解决方案**：
1. 使用 `pod update` 或 `swift package update` 更新依赖
2. 在 Podfile 中明确指定版本范围：

```ruby
pod 'CryptoSwift', '~> 1.5'
pod 'Tea', '~> 1.0'
pod 'AlibabaCloudCredentials', '~> 1.0'
```

### 4.12 安全性最佳实践

#### 问题 4.12.1：如何避免硬编码 AccessKey

**解决方案**：
1. **使用环境变量**：

```swift
let config = Config([
    "type": "access_key",
    "accessKeyId": ProcessInfo.processInfo.environment["ALIBABA_CLOUD_ACCESS_KEY_ID"],
    "accessKeySecret": ProcessInfo.processInfo.environment["ALIBABA_CLOUD_ACCESS_KEY_SECRET"]
])
```

2. **使用配置文件**（确保不提交到版本控制）：

```swift
// 从 .env 或 config.json 读取
func loadConfig() -> Config {
    guard let path = Bundle.main.path(forResource: "config", ofType: "json"),
          let data = try? Data(contentsOf: URL(fileURLWithPath: path)),
          let json = try? JSONSerialization.jsonObject(with: data) as? [String: String] else {
        fatalError("配置文件不存在")
    }
    
    return Config(json)
}
```

3. **生产环境使用 RAM 角色**：在 ECS 上运行时优先使用 `ecs_ram_role`，避免管理 AccessKey

#### 问题 4.12.2：如何安全地处理凭证对象

**解决方案**：
1. **不要打印完整凭证**：

```swift
// ✗ 危险
print("凭证: \(credential)")  // 可能泄露敏感信息

// ✓ 安全
print("凭证类型: \(credential?.getType() ?? "unknown")")
print("AccessKeyId 前缀: \(String(credential?.getAccessKeyId().prefix(8) ?? ""))")
```

2. **及时清理临时凭证**：

```swift
class SecureCredentialManager {
    private var credential: Credential?
    
    func clear() {
        credential = nil
    }
    
    deinit {
        clear()  // 对象销毁时清理
    }
}
```

---

## 5. 总结

本文档基于 Alibaba Cloud Credentials Swift SDK 的源码深度分析，涵盖了从安装配置到常见问题的完整使用指南。重点关注了代码中潜在的失败场景和异常处理逻辑，为用户提供了实用的问题解决方案。

**关键要点**：
1. 根据运行环境选择合适的凭证类型（本地开发用 AccessKey，ECS 用 RAM 角色）
2. 所有网络相关的凭证类型都可能超时，建议增加超时配置
3. SDK 使用了较多强制类型转换，务必用 try-catch 包裹所有凭证获取操作
4. 临时凭证会在过期前 180 秒自动刷新，无需手动管理
5. 生产环境避免硬编码 AccessKey，优先使用 RAM 角色或环境变量

**进一步帮助**：
- 官方文档：https://github.com/aliyun/credentials-swift
- 提交 Issue：https://github.com/aliyun/credentials-swift/issues
- OpenAPI 门户：https://next.api.aliyun.com/

---

**文档版本**：1.0  
**最后更新**：2024-12-07  
**适用 SDK 版本**：1.0.x
